<turbo-stream action="replace" target="build_moves_data">
    <template>
        <div id="build_moves_data" class="d-none" {% if new_percentages|length > 0 %}data-new-percentages="{{ new_percentages|json_encode }}"{% endif %}></div>
    </template>
</turbo-stream>